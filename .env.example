# Environment Variables

## Public (Client-side)

# Cloudflare Web Analytics
# Optional: Set to your Cloudflare beacon token to enable analytics on production.
# The beacon only loads when import.meta.env.PROD is true and this token is present.
# Leave blank to disable analytics.
# Staging: Leave blank (analytics disabled on staging).
# Production: Set to your production Cloudflare token (e.g., cfb_xxxxx).
PUBLIC_CLOUDFLARE_ANALYTICS_TOKEN=

# Google Site Verification (optional)
# Use this to claim your site in Google Search Console.
PUBLIC_GOOGLE_SITE_VERIFICATION=
